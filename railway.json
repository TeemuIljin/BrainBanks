{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && if [ \"$LOAD_COURSES_FIXTURES\" = \"1\" ]; then python manage.py loaddata fixtures/courses_data.json; fi && python manage.py collectstatic --noinput && gunicorn BrainBank.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
